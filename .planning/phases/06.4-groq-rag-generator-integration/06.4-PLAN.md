# Plan: Phase 06.4 - Groq RAG Generator Integration

## Wave 1: Tool Enhancement
- [x] Implement `astream_query` in `tools/groq_client.py`
  - [x] Add `json` and `re` imports if needed.
  - [x] Use `httpx.AsyncClient` with `stream=True`.
  - [x] Implement SSE parsing.
  - [x] Ensure compatibility with `GeminiClient` chunk structure.
- [ ] (Optional) Add `astream_query` stubs to other clients for consistency.

## Wave 2: Pipeline Refactoring
- [x] Refactor `core/rag_pipeline.py`
  - [x] Modify `__init__` to accept `provider` and `model_name`.
  - [x] Implement `_get_client(provider, model_name)`.
  - [x] Update `acustom_query` and `astream_query` to use `self.client`.
  - [x] Fix broken tests/eval scripts signatures.

## Wave 3: Integration & Config
- [x] Update `.env` with:
  - `GENERATION_PROVIDER=groq`
  - `GENERATION_MODEL=llama-3.1-70b`
- [x] Update `app.py` lifespan to use these env vars.
- [x] Create `tests/test_groq_integration.py` (verified via `test_rag_pipeline_provider.py`).
