---
wave: 2
depends_on: [05-01]
files_modified:
  - indexer.py
  - core/embeddings.py
  - db/qdrant.py
  - .env
autonomous: true
requirements:
  - ING-02
  - ING-03
---

# Plan 05-02: Hybrid Indexing & Context Injection

Integrate the new Legal Parser into the ingestion pipeline and enable Hybrid Search (Dense + Sparse) in Qdrant.

## Context
The indexer currently uses generic splitting. We need to switch to the `VietnameseLegalParser` and implement "Context Injection" for improved retrieval.

## Tasks

### 1. Update Qdrant Manager for Sparse Vectors
Enable sparse vector support in the Qdrant collection.
<action>
Update `db/qdrant.py`:
- In `init_collection`, add `sparse_vectors_config` using `models.SparseVectorParams`.
- Update `upsert` logic to accept both `vector` (dict with "dense" and "sparse") if provided.
</action>
<read_first>
- db/qdrant.py
- .planning/phases/05-hierarchical-structural-chunking/05-RESEARCH.md
</read_first>
<acceptance_criteria>
- Qdrant collection is recreated with sparse vector support.
</acceptance_criteria>

### 2. Implement Context Injection Logic
Implement the "Breadcrumb" injection before embedding.
<action>
Update `indexer.py` or create a helper:
- Create `prepare_chunk_for_indexing(node: LegalNode)` function.
- Injects `[{breadcrumb_path}] \n {content}`.
- Generates sparse vector (using `fastembed` or simple TF-IDF if locally managed).
</action>
<read_first>
- indexer.py
- .planning/phases/05-hierarchical-structural-chunking/05-CONTEXT.md
</read_first>
<acceptance_criteria>
- Chunk text in Qdrant contains the breadcrumb header.
</acceptance_criteria>

### 3. Refactor Indexer Loop
Switch the main indexer loop to use the new parser.
<action>
Modify `indexer.py`:
- Remove `MarkdownHeaderTextSplitter` and `RecursiveCharacterTextSplitter`.
- Use `LlamaParse` (via `llama_index.readers.llama_parse`) for Step 1 (PDF to MD).
- Use `VietnameseLegalParser` for Step 2 (MD to Hierarchical Chunks).
- Update `process_batch` to handle the new metadata schema (law_name, article, chapter).
</action>
<read_first>
- indexer.py
- .planning/phases/05-hierarchical-structural-chunking/05-RESEARCH.md
</read_first>
<acceptance_criteria>
- `indexer.py` runs successfully on a sample legal document.
- Metadata in Qdrant/SQLite is enriched with hierarchical data.
</acceptance_criteria>

### 4. Verification & Clean-up
Verify the full pipeline.
<action>
- Run a dry-run indexing of 5-10 documents.
- Inspect Qdrant payloads to verify breadcrumbs.
- Run `tests/test_retrieval.py` (if updated) to verify hybrid search impact.
</action>
<acceptance_criteria>
- Indexer progress is logged correctly.
- No "Context Fragmentation" observed in sample chunks.
</acceptance_criteria>
