---
phase: 03-smart-retrieval-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
    "core/classifier.py",
    "retrievers/sqlite_retriever.py",
    "retrievers/qdrant_retriever.py",
    "tests/test_classifier.py",
    "tests/test_retrieval.py"
]
autonomous: true
requirements: [CLS-01, CLS-02, RAG-01]

must_haves:
  truths:
    - "System can classify questions into 6 domains with confidence scores"
    - "System can apply multi-label filters to Qdrant searches"
    - "SQLite FTS5 results are correctly mapped to LlamaIndex Nodes"
  artifacts:
    - path: "core/classifier.py"
      provides: "Gemini-based multi-label query classification"
    - path: "retrievers/sqlite_retriever.py"
      provides: "Custom LlamaIndex retriever for SQLite FTS5"
  key_links:
    - from: "core/classifier.py"
      to: "retrievers/qdrant_retriever.py"
      via: "domain tags passed as MetadataFilters"
---

<objective>
Implement the intelligent query classification layer and specialized retrievers for the hybrid search pipeline.

Purpose: This plan establishes the "understanding" part of the RAG system, allowing it to focus searches on relevant legal domains and handle diverse query types.
Output: A multi-label classifier, a custom SQLite FTS5 retriever, and a filtered Qdrant retriever.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smart-retrieval-rag/03-CONTEXT.md
@.planning/phases/03-smart-retrieval-rag/03-AI-SPEC.md
@.planning/phases/03-smart-retrieval-rag/03-RESEARCH.md
@db/sqlite.py
@db/qdrant.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 - Environment and Test Stubs</name>
  <files>tests/test_classifier.py, tests/test_retrieval.py, requirements.txt</files>
  <read_first>.planning/phases/03-smart-retrieval-rag/03-VALIDATION.md</read_first>
  <action>
    1. Install dependencies: `pip install llama-index llama-index-llms-gemini llama-index-embeddings-huggingface llama-index-vector-stores-qdrant pydantic`.
    2. Create `tests/test_classifier.py` with stubs for classification logic.
    3. Create `tests/test_retrieval.py` with stubs for custom retriever checks.
  </action>
  <verify>pytest tests/test_classifier.py exits 0 (with collected items)</verify>
  <acceptance_criteria>
    - `requirements.txt` updated with LlamaIndex packages.
    - Test files exist in `tests/`.
  </acceptance_criteria>
  <done>Environment is ready and test structure is established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Multi-label Query Classifier</name>
  <files>core/classifier.py</files>
  <read_first>.planning/phases/03-smart-retrieval-rag/03-AI-SPEC.md</read_first>
  <action>
    1. Create `QueryClassification` Pydantic model with `domains: List[str]`, `confidence: float`, and `is_explicit_filter: bool`.
    2. Implement `LegalQueryClassifier` class using `Gemini` from `llama-index-llms-gemini`.
    3. Define the 6 legal domains in the system prompt: Civil & Family, Criminal, Business & Commercial, Labor & Insurance, Administrative & Tax, Land & Real Estate.
    4. Implement logic to return "General" domain if confidence is < 0.5.
  </action>
  <verify>pytest tests/test_classifier.py</verify>
  <acceptance_criteria>
    - `core/classifier.py` contains `QueryClassification` model.
    - Classifier correctly labels sample queries (e.g., "thủ tục ly hôn" -> "Civil & Family").
  </acceptance_criteria>
  <done>Queries can be categorized into legal domains with confidence scores.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SQLite FTS5 Custom Retriever</name>
  <files>retrievers/sqlite_retriever.py</files>
  <read_first>db/sqlite.py, .planning/phases/03-smart-retrieval-rag/03-RESEARCH.md</read_first>
  <action>
    1. Inherit from LlamaIndex `BaseRetriever`.
    2. Implement `_retrieve` method to query the `chunks_fts` table in SQLite.
    3. Map SQLite results to LlamaIndex `NodeWithScore` objects.
    4. Ensure the retriever accepts a `QueryBundle` and returns top-K results.
  </action>
  <verify>pytest tests/test_retrieval.py</verify>
  <acceptance_criteria>
    - `retrievers/sqlite_retriever.py` exists and implements `BaseRetriever`.
    - Retrieval returns correctly formatted LlamaIndex Nodes.
  </acceptance_criteria>
  <done>SQLite FTS5 is integrated into the LlamaIndex retrieval pipeline.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Filtered Qdrant Retriever Wrapper</name>
  <files>retrievers/qdrant_retriever.py</files>
  <read_first>db/qdrant.py, .planning/phases/03-smart-retrieval-rag/03-AI-SPEC.md</read_first>
  <action>
    1. Create a wrapper class that initializes `QdrantVectorStore` and `VectorStoreIndex`.
    2. Implement a method to build `MetadataFilters` from classification tags using `FilterOperator.IN`.
    3. Ensure it supports both Hard Filters (for explicit commands) and Soft Boosting (default) by adjusting similarity thresholds or weights.
  </action>
  <verify>pytest tests/test_retrieval.py</verify>
  <acceptance_criteria>
    - `retrievers/qdrant_retriever.py` can apply `MetadataFilters`.
    - Filtering works as expected (verified via test queries with specific metadata).
  </acceptance_criteria>
  <done>Vector search can be scoped to specific legal domains.</done>
</task>

</tasks>

<verification>
- [ ] `pytest tests/test_classifier.py` passes all cases.
- [ ] `pytest tests/test_retrieval.py` passes for both SQLite and Qdrant retrievers.
- [ ] No hardcoded API keys in the new files.
</verification>

<success_criteria>
- Multi-label classification is functional.
- Custom retrievers are ready for hybrid fusion in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-retrieval-rag/03-01-SUMMARY.md`
</output>
