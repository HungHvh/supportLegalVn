---
phase: 03-smart-retrieval-rag
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
    "core/rag_pipeline.py",
    "tests/test_generation.py",
    "scripts/eval_pipeline.py"
]
autonomous: true
requirements: [RAG-02, RAG-03]

must_haves:
  truths:
    - "RRF fusion correctly merges vector and keyword results"
    - "Generated answers follow IRAC structure and cite Article/Clause"
    - "RAGAS metrics are calculated and logged for the pipeline"
  artifacts:
    - path: "core/rag_pipeline.py"
      provides: "Unified Hybrid RAG pipeline with IRAC generation"
    - path: "scripts/eval_pipeline.py"
      provides: "Automated RAGAS evaluation suite"
  key_links:
    - from: "core/rag_pipeline.py"
      to: "retrievers/sqlite_retriever.py"
      via: "QueryFusionRetriever or custom RRF logic"
---

<objective>
Assemble the unified hybrid RAG pipeline, implement the IRAC-structured generation layer, and establish the evaluation flywheel.

Purpose: This plan completes the functional RAG system, turning retrieved legal data into expert-level Vietnamese legal advice with verifiable citations.
Output: A hybrid RAG pipeline, citation-aware generator, and evaluation script.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smart-retrieval-rag/03-01-SUMMARY.md
@.planning/phases/03-smart-retrieval-rag/03-AI-SPEC.md
@.planning/phases/03-smart-retrieval-rag/03-RESEARCH.md
@retrievers/sqlite_retriever.py
@retrievers/qdrant_retriever.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hybrid Retriever with RRF Fusion</name>
  <files>core/rag_pipeline.py</files>
  <read_first>retrievers/sqlite_retriever.py, retrievers/qdrant_retriever.py</read_first>
  <action>
    1. Implement `LegalHybridRetriever` using the RRF logic defined in `03-RESEARCH.md`.
    2. Set default weights: Keyword 0.5 - Vector 0.5.
    3. Ensure it uses the classification tags from Plan 01 to scope the vector search.
    4. Implement `top_k=8` results for the final fused set.
  </action>
  <verify>pytest tests/test_retrieval.py (update with hybrid cases)</verify>
  <acceptance_criteria>
    - Hybrid retriever merges results from both sources.
    - Resulting scores are normalized via RRF formula `1 / (k + rank)`.
  </acceptance_criteria>
  <done>Vector and Keyword search results are fused into a single ranked list.</done>
</task>

<task type="auto">
  <name>Task 2: Implement IRAC-Structured Generator</name>
  <files>core/rag_pipeline.py, tests/test_generation.py</files>
  <read_first>.planning/phases/03-smart-retrieval-rag/03-AI-SPEC.md</read_first>
  <action>
    1. Define the IRAC System Prompt in `core/rag_pipeline.py`.
    2. Implement `LegalResponseSynthesizer` that uses LlamaIndex's `CompactAndRefine` or `TreeSummarize` with the custom prompt.
    3. Enforce strict citation format: "Theo Khoản X, Điều Y, [Tên văn bản]...".
    4. Add a post-generation regex check to ensure citations are present.
  </action>
  <verify>pytest tests/test_generation.py</verify>
  <acceptance_criteria>
    - Generated answers follow IRAC (Issue, Rule, Analysis, Conclusion).
    - Citations are formatted correctly for Vietnamese law.
  </acceptance_criteria>
  <done>The system produces formal legal answers with citations.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument Evaluation and Tracing</name>
  <files>scripts/eval_pipeline.py, core/rag_pipeline.py</files>
  <read_first>.planning/phases/03-smart-retrieval-rag/03-RESEARCH.md</read_first>
  <action>
    1. Add Arize Phoenix instrumentation to the pipeline initialization.
    2. Create `scripts/eval_pipeline.py` using the `ragas` library.
    3. Implement metrics: `faithfulness`, `answer_relevancy`, `context_precision`.
    4. Create a small reference dataset (5 examples) to verify the eval script.
  </action>
  <verify>python scripts/eval_pipeline.py</verify>
  <acceptance_criteria>
    - `scripts/eval_pipeline.py` outputs a table of RAGAS scores.
    - Arize Phoenix app can be launched to view traces.
  </acceptance_criteria>
  <done>System performance is measurable and traceable.</done>
</task>

</tasks>

<verification>
- [ ] `pytest tests/test_generation.py` passes.
- [ ] `scripts/eval_pipeline.py` runs without errors.
- [ ] Visual check: Ensure disclaimer is included in generated answers.
</verification>

<success_criteria>
- End-to-end Hybrid RAG pipeline is functional.
- Answers are grounded, structured, and cited.
- Evaluation infrastructure is established.
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-retrieval-rag/03-02-SUMMARY.md`
</output>
