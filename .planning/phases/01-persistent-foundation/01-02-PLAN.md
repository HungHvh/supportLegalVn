---
phase: 01-persistent-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, db/sqlite.py, db/qdrant.py, core/embeddings.py]
autonomous: true
requirements: [INF-01, INF-02]
---

<objective>
Implement the optimized database layer and the flexible embedding provider interface.

Purpose: To provide a high-performance storage layer for 3.6GB of legal data and future-proof the embedding logic.
Output: Decoupled database modules and an Abstract Base Class for embedding providers.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistent-foundation/01-CONTEXT.md
@.planning/phases/01-persistent-foundation/01-RESEARCH.md
@.planning/phases/01-persistent-foundation/01-PATTERNS.md
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Requirements for Backend Development</name>
  <files>requirements.txt</files>
  <action>
    Append the following libraries to requirements.txt:
    - pydantic-settings
    - python-dotenv
    - google-generativeai
  </action>
  <verify>grep "pydantic-settings" requirements.txt</verify>
  <acceptance_criteria>
    - requirements.txt contains pydantic-settings, python-dotenv, and google-generativeai.
  </acceptance_criteria>
  <done>Backend dependencies are added</done>
</task>

<task type="auto">
  <name>Task 2: Implement SQLite Layer with WAL and FTS5 Optimization</name>
  <files>db/sqlite.py</files>
  <read_first>main.py, .planning/phases/01-persistent-foundation/01-RESEARCH.md</read_first>
  <action>
    Create `db/sqlite.py` with:
    1. Connection manager that enables `PRAGMA journal_mode = WAL;` and `PRAGMA mmap_size = 3000000000;`.
    2. Schema initialization logic that creates:
       - `legal_documents` (primary table for metadata).
       - `docs_fts` (FTS5 virtual table using 'external content' from `legal_documents`).
  </action>
  <verify>python -c "from db.sqlite import get_db; get_db()" creates file and sets WAL</verify>
  <acceptance_criteria>
    - db/sqlite.py exists
    - Contains WAL mode PRAGMA
    - Contains FTS5 external content table definition
  </acceptance_criteria>
  <done>Optimized SQLite layer is implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Embedding Provider Interface</name>
  <files>core/embeddings.py</files>
  <read_first>.planning/phases/01-persistent-foundation/01-RESEARCH.md</read_first>
  <action>
    Create `core/embeddings.py` with:
    1. `BaseEmbedder` Abstract Base Class (ABC) with `embed_text` and `embed_batch` methods.
    2. `VietnameseSBERTProvider` implementation using `SentenceTransformer("keepitreal/vietnamese-sbert")`.
  </action>
  <verify>python -c "from core.embeddings import VietnameseSBERTProvider; print(VietnameseSBERTProvider)"</verify>
  <acceptance_criteria>
    - core/embeddings.py contains an ABC and a concrete class
    - get_embedding returns a list of floats
  </acceptance_criteria>
  <done>Decoupled embedding layer is ready</done>
</task>

</tasks>

<verification>
- [ ] SQLite database file shows WAL mode enabled (journal file exists during write).
- [ ] FTS5 table is confirmed as external content via `.schema` check.
- [ ] `core/embeddings.py` passes import and basic instantiation check.
</verification>

<success_criteria>
- Application has a dedicated, optimized database module.
- Embedding logic is abstracted behind a clean interface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistent-foundation/01-02-SUMMARY.md`
</output>
