---
phase: 01-persistent-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml, .env.example, .gitignore]
autonomous: true
requirements: [INF-03, API-03]
---

<objective>
Set up the persistent infrastructure using Docker Compose and establish a secure environment variable system.

Purpose: To ensure Qdrant data persists across restarts and provide a unified network for the supportLegal services.
Output: A functional docker-compose.yml with Qdrant persistence and a validated .env setup.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistent-foundation/01-CONTEXT.md
@.planning/phases/01-persistent-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Docker Compose for Qdrant Persistence</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml, .planning/phases/01-persistent-foundation/01-RESEARCH.md</read_first>
  <action>
    Update docker-compose.yml to:
    1. Define a service `qdrant` using `qdrant/qdrant:latest`.
    2. Map host directory `./qdrant_data` to `/qdrant/storage` in the container.
    3. Expose ports 6333 (REST) and 6334 (gRPC).
    4. Connect to a custom bridge network named `legal-network`.
    5. Set restart policy to `unless-stopped`.
  </action>
  <verify>docker-compose config passes validation</verify>
  <acceptance_criteria>
    - docker-compose.yml contains service 'qdrant'
    - volume mapping includes './qdrant_data:/qdrant/storage'
    - network 'legal-network' is defined
  </acceptance_criteria>
  <done>Docker Compose is ready for persistent Qdrant</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Environment and Gitignore</name>
  <files>.env.example, .gitignore</files>
  <read_first>.gitignore, .planning/phases/01-persistent-foundation/01-CONTEXT.md</read_first>
  <action>
    1. Create `.env.example` with placeholders for:
       - `GEMINI_API_KEY`
       - `HUGGINGFACE_TOKEN`
       - `QDRANT_URL=http://localhost:6333`
       - `SQLITE_DB_PATH=legal_poc.db`
    2. Ensure `.planning/`, `.env`, and `qdrant_data/` are in `.gitignore`.
  </action>
  <verify>ls -a shows .env.example; grep checks .gitignore</verify>
  <acceptance_criteria>
    - .env.example exists with all 4 keys.
    - .gitignore contains 'qdrant_data/' and '.env'.
  </acceptance_criteria>
  <done>Environment configuration is established</done>
</task>

</tasks>

<verification>
- [ ] `docker-compose config` succeeds.
- [ ] `.env.example` contains all required project keys.
- [ ] `.gitignore` correctly excludes local data and secrets.
</verification>

<success_criteria>
- Project has a valid Docker Compose setup for persistent storage.
- Environment variable structure is defined and secured.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistent-foundation/01-01-SUMMARY.md`
</output>
