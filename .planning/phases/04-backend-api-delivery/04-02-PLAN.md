# Plan: 04-02 API Endpoints & Streaming

**Goal:** Implement the structured `/ask` endpoint and the streaming SSE variant.

## User Review Required

> [!TIP]
> The streaming endpoint will use a custom JSON-per-line format within the SSE stream so that the final chunk can deliver metadata (citations) while earlier chunks deliver tokens.

## Proposed Changes

### [API Layer]

#### [NEW] [api/v1/endpoints.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/api/v1/endpoints.py)
- Implement `/api/v1/ask` (POST) for structured JSON.
- Implement `/api/v1/stream` (POST) for SSE streaming.
- Implement `/api/v1/health` (GET) for system status.

#### [NEW] [api/models.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/api/models.py)
- Pydantic models for `AskRequest`, `AskResponse`, `Citation`, etc.

### [Integration]

#### [MODIFY] [app.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/app.py)
- Include the v1 router.
- Inject `LegalRAGPipeline` into routes via `app.state`.

## Verification Plan

### Automated Tests
- `pytest tests/test_api_endpoints.py` — Test `/ask` with various queries.
- `pytest tests/test_streaming.py` — Test that `/stream` returns valid SSE frames and a final metadata chunk.

### Manual Verification
- Use `curl` or a tool like `Postman` to verify the SSE stream:
  `curl -X POST -H "Content-Type: application/json" -d '{"query": "..."}' http://localhost:8000/api/v1/stream`
