# Plan: 04-01 Core Infrastructure & Async Refactoring

**Goal:** Establish the FastAPI boilerplate and refactor the existing RAG pipeline to be fully asynchronous.

## User Review Required

> [!IMPORTANT]
> This plan involves significant refactoring of the `core/` and `retrievers/` modules to use `async` patterns. This is necessary for FastAPI performance but will break any existing synchronous scripts (like `main.py` prototype).

## Proposed Changes

### [Infrastructure]

#### [MODIFY] [requirements.txt](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/requirements.txt)
- Add `fastapi`, `uvicorn`, `sse-starlette`, `httpx`.

#### [NEW] [app.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/app.py)
- Implement FastAPI app initialization.
- Implement `lifespan` for singleton management (Database/Qdrant/LLM).
- Configure `CORSMiddleware` with environment variables.

### [Core Refactoring]

#### [MODIFY] [core/classifier.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/core/classifier.py)
- Ensure `classify` is a proper `async` function (if not already).

#### [MODIFY] [retrievers/sqlite_retriever.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/retrievers/sqlite_retriever.py)
- Wrap SQLite calls in `run_in_executor` or use an async sqlite library (like `aiosqlite`) if preferred. For this POC, we'll start with `run_in_executor` to keep the SQLite setup simple.

#### [MODIFY] [retrievers/qdrant_retriever.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/retrievers/qdrant_retriever.py)
- Use `qdrant_client.AsyncQdrantClient` instead of the synchronous version.

#### [MODIFY] [core/rag_pipeline.py](file:///c:/Users/hvcng/PycharmProjects/supportLegalVn/core/rag_pipeline.py)
- Update `LegalHybridRetriever` to use `async def _aretrieve`.
- Update `LegalRAGPipeline` to use `async def acustom_query`.

## Verification Plan

### Automated Tests
- `pytest tests/test_async_retrieval.py` — Verify that async retrieval returns the same results as sync.
- `pytest tests/test_lifespan.py` — Verify that database connections are correctly initialized and closed.

### Manual Verification
- Run `uvicorn app:app --reload` and check `/docs` to ensure the server starts and the lifespan logic completes without errors.
